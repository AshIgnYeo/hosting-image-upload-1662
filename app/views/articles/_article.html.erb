<div id="<%= dom_id article %>">

  <%= cl_image_tag(article.photo.key, height: 300, width: 200, crop: :thumb) if article.photo.attached? %>
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>


  <p>
    <strong>Body:</strong>
    <%= article.body %>
  </p>

</div>
